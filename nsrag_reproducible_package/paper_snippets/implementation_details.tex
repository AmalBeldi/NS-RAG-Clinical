\paragraph{Implementation Details.}
The prototype is implemented in Python using NetworkX for patient-centered graph construction, RDFLib for RDF conversion, and scikit-learn for textual retrieval baselines. The released implementation includes a deterministic generator to ensure reproducibility without external API dependencies; this module can be replaced with any LLM backend. All methods are evaluated using the same patient instances, prompt templates, and evaluation scripts.
