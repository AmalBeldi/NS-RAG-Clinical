\paragraph{Reproducibility.}
To support reproducibility, we release a lightweight implementation of the experimental protocol, including graph construction, RDF conversion, graph-grounded retrieval, verification, explanation extraction, evaluation scripts, and a synthetic de-identified EHR-like dataset following the same schema as the preprocessed MIMIC-III/i2b2 instances. The code produces CSV files, LaTeX tables, and figures from a single command.
